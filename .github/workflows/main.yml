name: Validar_Entrega
on: [push]

jobs:
  revisar_entorno:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Mensaje de confirmacion
        run: echo "El flujo de CI funciona correctamente. Tarea entregada."
